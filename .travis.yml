language: node_js

node_js:
  - "6"

addons:
  firefox: "latest"

branches:
  only:
  - master

env:
  global:
  - SAUCE_USERNAME=youhp
  - SAUCE_ACCESS_KEY=eefb2f82-1299-421d-839f-2630b7357069
  - BROWSER_PROVIDER_READY_FILE=/tmp/sauce-connect-ready
  - LOGS_DIR=/tmp/logs

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install

before_script:
  - mkdir -p $LOGS_DIR
  - chmod a+x ./scripts/sauce_connect_setup.sh
  - ./scripts/sauce_connect_setup.sh

script:
  - sh ./scripts/sauce_connect_block.sh
  - ng e2e
  #- ng build
  #- gulp jigsaw:build-release:clean
  #- ng test --code-coverage --watch false
  #- cat ./coverage/lcov.info | ./node_modules/.bin/coveralls

after_script:
  - sh ./scripts/sauce_connect_teardown.sh

sudo: false

